[package]
name = "hello-actor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
packr-guest = { version = "0.5.4", features = ["derive"] }

[profile.release]
opt-level = "s"
lto = true
